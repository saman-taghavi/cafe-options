# Café Agents

This workspace provides commands and scripts for curating new cafés.

To add a new café quickly:
```bash
pnpm cafe:new <slug>
```
Then fill out the generated json.

To curate via an LLM with structured instruction, create a prompt like:
"I want to add a cafe called roberto in pasdaran... please format it"
